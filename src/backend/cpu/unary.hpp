#include <Array.hpp>
#include <optypes.hpp>
#include <math.hpp>
#include <err_cpu.hpp>

namespace cpu
{

    template<typename T, af_op_t op>
    Array<T>* unaryOp(const Array<T> &in)
    {
        CPU_NOT_SUPPORTED();
    }

}
